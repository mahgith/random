# Core KFP + Vertex AI
kfp==1.8.22                       # Kubeflow Pipelines SDK (v1 API, compatible with Vertex AI)
google-cloud-aiplatform>=1.38.0   # Vertex AI Python SDK
google-cloud-bigquery>=3.0.0      # BigQuery data source
google-cloud-storage>=2.0.0       # GCS operations
db-dtypes>=1.0.0                  # BQ → pandas type conversions

# Data / ML
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0                   # Parquet support
scikit-learn>=1.3.0
joblib>=1.3.0

# Forecasting models
lightgbm>=4.0.0
prophet>=1.1.5

# Holiday calendars
holidays>=0.46

# Configuration
pyyaml>=6.0

# Development / testing
pytest>=7.0.0
jupyter>=1.0.0
ipykernel>=6.0.0
