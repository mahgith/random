# Core KFP + Vertex AI
kfp>=2.9.0                        # Kubeflow Pipelines SDK v2
google-cloud-aiplatform>=1.70.0   # Vertex AI Python SDK
google-cloud-bigquery[pandas,pyarrow]>=3.25.0  # BigQuery data source + DataFrame support
google-cloud-storage>=2.18.0      # GCS operations
db-dtypes>=1.3.0                  # BQ → pandas type conversions

# Data / ML
pandas>=2.2.3
numpy>=1.26.4
pyarrow>=18.0.0
scikit-learn>=1.6.0
joblib>=1.4.2

# Forecasting models
lightgbm>=4.5.0
prophet>=1.1.5

# Holiday calendars
holidays>=0.60

# Configuration + settings
pyyaml>=6.0.2
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Structured logging
structlog>=25.1.0

# Serving container (docker/serving only — not needed for pipeline runner)
# fastapi>=0.115.0
# uvicorn>=0.34.0

# Development / testing
pytest>=8.0.0
jupyter>=1.0.0
ipykernel>=6.0.0
